# Backend environment variables (example values; do NOT commit real keys)

# Default listen port
PORT=3001

# Groq (Llama) API configuration
# Obtain your API key from https://console.groq.com/ and set a valid model.
# Example models change over time; check Groq docs for current vision-capable models.
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Optional tuning
GROQ_TIMEOUT_MS=25000
GROQ_MAX_TOKENS=1100

# Optional warm-up on server start (true/false)
LLAMA_WARMUP_ON_START=false
