# Backend environment variables (do NOT commit real keys)

# Google Gemini API key (mantener solo local)
GEMINI_API_KEY=

# Nota: los nombres de modelos disponibles pueden variar por cuenta y versión de API.
# Si el modelo configurado no existe, el backend intentará listar modelos y elegir uno compatible automáticamente.
GEMINI_MODEL=gemini-2.0-flash

# Timeout duro para Gemini (ms). Si se excede, el backend hace fallback a stub.
GEMINI_TIMEOUT_MS=25000

# Puerto por defecto del backend
PORT=3001
